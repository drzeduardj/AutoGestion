PORT=4000
HOST=0.0.0.0
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taller_automotriz
DB_USER=postgres
DB_PASSWORD=TU_PASSWORD
JWT_SECRET=una_clave_segura
JWT_EXPIRES_IN=8h
UPLOAD_DIR=uploads
CORS_ORIGIN=*

# --- Despliegue en Vercel (opcional) ---
# Si existe DATABASE_URL se ignoran las variables DB_*.
# DATABASE_URL=postgresql://usuario:password@host/db?sslmode=require
# DB_SSL=true
# Token de Vercel Blob para guardar las fotos (se crea al conectar un Blob store al proyecto).
# BLOB_READ_WRITE_TOKEN=
