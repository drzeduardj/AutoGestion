{
  "name": "autogestion",
  "version": "0.1.0",
  "private": true,
  "description": "Gestion de taller automotriz (backend API + frontend web).",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "migrate": "npm --prefix backend run migrate",
    "clean:inventario": "npm --prefix backend run clean:inventario",
    "start": "npm --prefix backend run start",
    "start:prod": "npm --prefix frontend run build && npm --prefix backend run start",
    "setup": "npm run install:all && npm run build && npm run migrate"
  }
}
